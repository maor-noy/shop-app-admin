import { FC } from "react";
import { Category } from "@/app/admin/categories/categories.types";

type Props = {
    categories: Category[];
}
export const ProductPageComponnnt: FC<Props> = ({}) => {
  return (
    <div>ProductPageComponnnt</div>
  )
}
